name: Hello World
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World Action
        run: echo "Hello, World!"
      - name: Prints Log
        run: echo "Prints log!"
